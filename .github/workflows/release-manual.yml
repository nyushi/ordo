name: Release (manual)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to build"
        required: true
        type: string

jobs:
  release:
    uses: ./.github/workflows/release-app.yml
    with:
      tag: ${{ inputs.tag }}
    secrets: inherit
